@model IEnumerable<CampusHub.Web.DTOs.RoomReservationDto>

<h2>My Reservations</h2>

<table class="table">
@foreach (var item in Model)
{
    <tr>
        <td>@item.RoomName</td>
        <td>@item.ReservationDate</td>
        <td>@item.Status</td>
    </tr>
}
</table>